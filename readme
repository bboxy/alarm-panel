Einrichtung:
mount einrichten für .tif/.pdf vom fax/fritzbox/
sudo cp apacheconfig/000-default.conf /etc/apache/sites/available/000-default.conf
000-default.conf anpassen (vor allem document root)
sudo cp alarmon.cfg /etc/
alarmmon.cfg anpassen falls nötig
sudo cp alarmmon.init /etc/init.d/
sudo update-rc.d /etc/init.d/alarmmon.init defaults
sudo /etc/init.d/alarmmon.init start
#ils.traineddata kopieren falls Tesseract benutzt wird, wohin? /usr/lib/tesseract?
